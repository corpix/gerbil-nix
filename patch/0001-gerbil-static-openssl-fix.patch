diff --git src/build/build-bach.ss src/build/build-bach.ss
index 18ac42a7..57335652 100644
--- src/build/build-bach.ss
+++ src/build/build-bach.ss
@@ -31,6 +31,9 @@
  (else
   (def default-ld-options ["-ldl" "-lm"])))
 
+(when (member "--enable-openssl" (string-split (configure-command-string) #\'))
+  (set! default-ld-options (append default-ld-options ["-lssl" "-lcrypto"])))
+
 (def builtin-modules
   '(;; :gerbil/runtime
     "gerbil/runtime/gambit"
diff --git src/std/build-spec.ss src/std/build-spec.ss
index c2cd1ada..ebebca19 100644
--- src/std/build-spec.ss
+++ src/std/build-spec.ss
@@ -208,18 +208,14 @@
         '())
     ;; :std/net
     "net/address"
-    ,(cond-expand
-      (darwin
-       `(gxc: "net/ssl/libssl"
-	      "-cc-options" ,(cppflags "libssl" "")
-	      "-ld-options" ,(apply append-options
-			               (ldflags "libssl" "-lssl")
-			               (ldflags "libcrypto" "-lcrypto")
-                           (if (enable-shared?)
-                             [(string-append "-L" (gerbil-libdir)) "-lgambit"]
-                             []))))
-      (else `(gxc: "net/ssl/libssl"
-		   "-ld-options" ,(ldflags "libssl" "-lssl"))))
+    ,`(gxc: "net/ssl/libssl"
+            "-cc-options" ,(cppflags "libssl" "")
+		    "-ld-options" ,(apply append-options
+			                 (ldflags "libssl" "-lssl")
+			                 (ldflags "libcrypto" "-lcrypto")
+                             (if (enable-shared?)
+                               [(string-append "-L" (gerbil-libdir)) "-lgambit"]
+                               [])))
     "net/ssl/error"
     "net/ssl/interface"
     "net/ssl/socket"
